# Jarvis Hermes Lab Note — 2026-07-27 (Pazartesi)

## Bugünün Kısa Hedefi
26 Temmuz notundaki plana devam ettim: `example_catalog_indexer.py --strict`
çıktısında kalan 18 `missing-usage` dosyadan üçüne — tam olarak dünkü notta
adı geçen `action_review.py`, `approval_matrix.py`, `safe_command_router.py` —
gerçek bir `Usage:` bloğu ekledim. Yeni özellik yok, kod mantığı değişmedi;
sadece kataloğun kendi raporladığı somut bir tutarsızlık azaldı.

## Mimari Notu
`example_catalog_indexer.py` bir dosyayı `ok` olarak işaretlemek için üç şeyi
birlikte istiyor: docstring başlığı, `^\s*Usage:\s*$` düzenli ifadesine uyan
bir satır, ve README "Safe Examples" tablosunda bir giriş. Bugün seçtiğim üç
dosyanın (`action_review.py`, `approval_matrix.py`, `safe_command_router.py`)
üçü de zaten README'de listeliydi ve docstring başlığına sahipti — tek eksik
`Usage:` bloğuydu, yani en düşük riskli/en dar kapsamlı düzeltmeydi (README
tablosunu değiştirmeye gerek yok, sadece docstring'e üç örnek komut satırı
eklemek yeterli). Her Usage bloğu, o script'in gerçek CLI argümanlarını
(`--title`, `--format json`, `--command`) örnekliyor; script'i hiç çalıştırmadan
okuyan birinin ne beklemesi gerektiğini gösteriyor.

Gerçek çalıştırma: değişiklikten önce `--strict` "35 dosya: 17 ok, 18
mismatch" raporladı; değişiklikten sonra "35 dosya: 20 ok, 15 mismatch" —
üç dosya `missing-usage`'dan `ok`'a geçti, kalan 15 mismatch tamamı hâlâ
`missing-usage` (bugün hepsini bitirme iddiası yok, kademeli devam ediyor).

## Küçük Yapılabilir Görev
- [x] `examples/action_review.py` docstring'ine `Usage:` bloğu ekle
      (üç gerçek CLI örneği: `read_only`, `external_publish --title ...`,
      `device_control --format json`)
- [x] `examples/approval_matrix.py` docstring'ine `Usage:` bloğu ekle
- [x] `examples/safe_command_router.py` docstring'ine `Usage:` bloğu ekle
      (bir örnek de `--command "share my api key"` ile `blocked` yolunu
      göstermek için seçildi — sensitive-terms filtresinin gerçekten
      çalıştığını doğrulamak amacıyla)
- [x] `python3 -m py_compile` ile üç dosyanın syntax'ını doğrula
- [x] Her üç script'i gerçek argümanlarla çalıştırıp çıktıyı gözle kontrol et
- [x] `python3 examples/example_catalog_indexer.py --strict` ile öncesi/sonrası
      farkı doğrula (18 → 15 mismatch, üçü de `ok`)
- [x] `python3 examples/public_safety_scan.py .` ile tüm repoyu tara
      (temiz çıktı)
- [ ] Gelecek gün: kalan 15 `missing-usage` dosyadan 2-3'üne devam et
      (öneri: `config_template_validator.py`, `dashboard_mock_data.py`,
      `notification_digest.py` — üçü de zaten README'de var ve docstring
      başlığına sahip, sadece Usage bloğu eksik)

## Gelecekteki Modül Fikri
**Modül: `usage_block_lint_ci`**
Bugüne kadar `missing-usage` düzeltmesi hep elle, bir günde 2-3 dosya olarak
yapılıyor. Kalan sayı sıfıra indiğinde (tahminen bir-iki hafta içinde), bu
gelecekteki modül `example_catalog_indexer.py --strict`'i basit bir GitHub
Actions job'ı olarak çalıştırıp, yeni bir `examples/*.py` dosyası `Usage:`
bloğu veya README satırı olmadan eklenirse PR'ı `mismatch` durumuyla
işaretleyebilir — böylece drift'in tekrar birikmesi (18'e geri çıkması)
otomatik olarak yakalanır. Şimdilik sadece bir fikir; workflow dosyası
henüz yazılmadı.

## Güvenlik / Secret Hatırlatması
- Bugün değiştirilen üç dosya (`examples/action_review.py`,
  `examples/approval_matrix.py`, `examples/safe_command_router.py`) sadece
  docstring'e örnek CLI komut satırları ekledi — hiçbiri token, API key,
  gerçek dosya yolu, ev ağı adresi veya kişisel veri içermiyor.
- `safe_command_router.py` örneğindeki `--command "share my api key"` satırı
  gerçek bir anahtar değil; script'in `blocked_for_public_repo` yolunu
  tetiklediğini göstermek için kasıtlı seçilmiş sahte bir ifade.
- `python3 examples/public_safety_scan.py .` tüm repo üzerinde çalıştırıldı
  ve "No high-signal public-safety findings were detected" sonucu alındı.
- Commit öncesi `git diff --stat` ile yalnızca beklenen dört dosyanın
  (üç script + bu not) değiştiği doğrulandı.

## Durum
- Tüm daily notlar ✅ mevcut (2026-07-17 → 2026-07-27)
- Bugün eklenen: `docs/daily/2026-07-27.md`
- Bugün güncellenen: `examples/action_review.py`,
  `examples/approval_matrix.py`, `examples/safe_command_router.py`
  (her birine `Usage:` bloğu eklendi)
